// const gcs = require("../lib/googleCloudStorage");

// async function uploadFile(file) {
//   if (!file) throw new Error("No file provided");

//   return gcs.uploadFile(file);
// }

// module.exports = { uploadFile };
